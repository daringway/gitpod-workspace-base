#!/usr/bin/bash

ENV_PYTHON_VERSION=${ENV_PYTHON_VERSION:-latest}

curl https://pyenv.run | bash

echo '
export PYENV_ROOT="$HOME/.pyenv"
command -v pyenv >/dev/null || export PATH="$PYENV_ROOT/bin:$PATH"
eval "$(pyenv init -)"
' >> ~/.bash_profile

pyenv install ${ENV_PYTHON_VERSION}:latest
